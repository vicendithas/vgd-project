using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class ObjectDatabase {

	// Use this for initialization
	void Start ()
	{
		// initial goal for this script is to hold all of the weapon, shield, spell, and enemy data that's read in via XML for public reference
		
		// since it's a database, it does not need to inherit MonoBehaviour
	
		//Dictionary<string, BadGuy> badguys = new Dictionary<string, BadGuy>();
		
	}
	
	// Update is called once per frame
	void Update ()
	{
	
	}
}
